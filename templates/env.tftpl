# Generated by azure-cdf-bootstrap (terraform). Do not commit.
# Entra security group: ${group_name} (object id below -> use as sourceId on the CDF group)

IDP_TENANT_ID=${tenant_id}
IDP_CLIENT_ID=${client_id}
IDP_CLIENT_SECRET=${client_secret}
IDP_TOKEN_URL=https://login.microsoftonline.com/${tenant_id}/oauth2/v2.0/token
IDP_SCOPES=https://${cdf_cluster}.cognitedata.com/.default

CDF_CLUSTER=${cdf_cluster}
CDF_PROJECT=${cdf_project}
CDF_URL=https://${cdf_cluster}.cognitedata.com

ENTRA_GROUP_ID=${group_id}
